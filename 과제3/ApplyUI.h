#include <iostream>
#include <vector>
#include "JobPosting.h"
// 채용 지원 화면을 위한 ApplyUI 클래스 정의
class ApplyUI
{
private:

public:
    ApplyUI();
    string showInterface(ifstream& inputFile);
};
